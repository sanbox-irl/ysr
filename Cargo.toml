[package]
name = "ysr"
version = "0.1.0"
edition = "2021"

[features]
default = ["rand"]

[dependencies]
prost = "0.11"
rand = { version = "0.8.5", optional = true }
thiserror = "1.0"

[dev-dependencies]
dialoguer = "0.10.3"
